# InfiniPHY Simulator Assignment

To execute, open a terminal in the project directory and pass the target assembly file to the main script.

To compute the standard IPv4 checksum:
```bash
python main.py checksum.asm
```

Note: The execution loop will pause after every cycle. Press Enter to step forward and view the system state.